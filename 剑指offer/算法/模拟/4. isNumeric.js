/**
 * 代码中的类名、方法名、参数名已经指定，请勿修改，直接返回方法规定的值即可
 *
 *
 * @param str string字符串
 * @return bool布尔型
 *
 * 表示数值的字符串
 *
 * 输入："123.45e+6"
 * 返回值：true
 */
function isNumeric(str) {
    // write code here
    return /^[+-]?(\d+\.?\d*|\.\d+)([eE][+-]?\d+)?$/.test(s.trim())
}
// ^[+-]?可选正负号
// (\d+\.?\d*|\.\d+) → 整数部分（如 123）或小数部分（如 .123、123.、123.456）
// ([eE][+-]?\d+)?可选的指数部分（如 e+10）
// $ 是锚点，强制匹配到字符串末尾
